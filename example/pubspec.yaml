name: flutter_live_example
description: Demonstrates how to use the flutter_live plugin.
publish_to: 'none'

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_live:
    path: ../

flutter:

  uses-material-design: true
