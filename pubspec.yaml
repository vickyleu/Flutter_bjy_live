name: flutter_live
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: any #">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    androidPackage: com.xgs.flutter_live
    pluginClass: FlutterLivePlugin
