name: flutter_live
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.xgs.flutter_live
        pluginClass: FlutterLivePlugin
      ios:
        pluginClass: FlutterLivePlugin
